{
    "build": { "dockerfile": "Dockerfile" },

    "customizations": {
        "vscode": {
            "extensions": ["github.copilot","github.copilot-chat","redhat.vscode-yaml","ms-azuretools.vscode-docker","ms-vscode.makefile-tools"]
        }
    },

    "features": {
        "ghcr.io/devcontainers/features/go:1": {}
    },

    "mounts": [
        "type=bind,source=${localEnv:HOME}/.ssh,target=/root/.ssh,readonly"
    ]
}
